{% extends 'base.html.twig' %}
{# 
{% block title %}{% endblock %}

{% block meta %}
{% endblock meta %}
 #}
{% block main %}
	<h1>ФизМат БрГУ: Расписание</h1>
	<div class="row main">
		{% for course in shedule.courses %}
			<div class="col-xs-12 col-sm-6 col-md-4 col-lg-3 btn-course">
				<a href="{{path('course', {'url': course.url})}}" class="btn btn-dark btn-huge btn-block">{{course.name}}</a>	
			</div>
		{% endfor %}
	</div>
{% endblock %}